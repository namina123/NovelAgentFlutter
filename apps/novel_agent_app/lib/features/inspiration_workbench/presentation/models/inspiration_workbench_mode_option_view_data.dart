class InspirationWorkbenchModeOptionViewData {
  const InspirationWorkbenchModeOptionViewData({
    required this.id,
    required this.title,
    required this.description,
    required this.isSelected,
  });

  final String id;
  final String title;
  final String description;
  final bool isSelected;
}
