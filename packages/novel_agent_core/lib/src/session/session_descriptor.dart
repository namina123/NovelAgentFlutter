class SessionDescriptor {
  const SessionDescriptor({
    required this.id,
    required this.projectId,
    required this.title,
  });

  final String id;
  final String projectId;
  final String title;
}
