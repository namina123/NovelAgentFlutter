/// 开放叙事状态使用的命名空间字符串。
///
/// 具体 claim / profile / ledger 模型将在后续 session 引入。
typedef NarrativeStateNamespace = String;

/// 开放叙事状态相关合同使用的 schema version 字符串。
typedef NarrativeStateSchemaVersion = String;

/// 叙事状态中的稳定对象标识，例如 claim、profile、ledger entry 的 id。
typedef NarrativeStateRecordId = String;
