我顺手跑了整包 packages/novel_agent_adapters 的全量测试，里面还有几条旧失败，落在 openai_llm_gateway_test.dart 和 project_tool_dispatcher_path_test.dart，看起来是这轮之外的既有问题，不是 long_task_station 这条线引入的。

packages/novel_agent_core 全量 dart test 里仍有一个与本轮无关的既有失败，落在 test/agent_services_test.dart，是标签归一化预期不一致，不是这轮 baseline 改动引入的。

下一步最自然的就是把 adapter 侧也接上：让 review 结果自动判定是否生成 repair task，并在确实需要返工时真正阻塞下一章，而不是现在先把 gate 合同立在 core。

session 9:我这轮刻意没去碰 UI 打磨，只把领域流程闭环做实了。后面最自然的下一步，就是让 app/CLI 在启动长任务时真正读取这个 runtime_profile.json，而不是只靠临时参数往下传。

后续扩展点我也给你留清楚了：

真实导入写入流程：在现有预检之上补 preview -> write plan -> apply
adapter 层导出实现：目录导出 / 未来 zip 导出
老的 bundle 服务与新 bundle 基座收敛复用
GUI/CLI 接 bundle 入口时，直接消费现在这套 preview / validation 合同


补充一点：apps/novel_agent_app 整体 flutter analyze 仍有仓库里原本就存在的无关测试桩错误，我没有在这轮把范围扩散过去；ONS-40 还没开始。